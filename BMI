#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--)
	{
	    int m,h;
	    cin>>m>>h;
	    int b=(m/(h*h));
	    if (b<=18)cout<<1;
	    else if (b>=19 && b<=24)cout<<2;
	    else if (b>=25 && b<=29)cout<<3;
	    else cout<<4;
	    cout<<endl;
	}
	return 0;
}
